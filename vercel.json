{
  "buildCommand": "npm run build --prefix web",
  "outputDirectory": "web/dist",
  "devCommand": "npm run dev --prefix web",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
